node_modules

# Local Netlify folder
.netlify

# Environment variables (contain secrets)
.env
.env.local
.env*.local

*storybook.log
storybook-static
notification_catalog/site/.astro/

# Local vitest helper artifacts
.vitest-unit.*

# macOS junk
.DS_Store

# Expo / React Native local state (keep out of git)
.expo/
apps/**/.expo/

# Expo local env (contains keys / device-specific URLs)
**/env.local
