# Expo — next small fixes update

Parked minor items ready for a **live/hotfix** ship (not Major).

## Ready (local WIP on `expo-ui-carl`)

1. **ML GW + Season tables — tied ranks**  
   Same standing → `1=` (competition ranks), matching main leaderboards / web.

2. **ML GW + Season tables — profile pics**  
   Avatar (or initials) next to each player name.

3. **ML GW + Season tables — styling parity**  
   GW matched to Season (muted headers, row height, avatar chrome); Season header top line removed; header unicorn ~20% larger.

## Hold for Major (`feat/prediction-card-stats`)

- Flip card / stats back / match preview API cache — **do not** include in the small fixes update.

## Notes

- Prefer shipping the three ML table items alone after GW live pressure eases.
- Keep flip work on a feature branch; do not merge into `expo-ui-carl` until Major.
