/**
 * Shared layout constants for consistent screen spacing.
 *
 * We use a floating bottom tab bar (pill) that sits above the bottom edge.
 * Screens with vertical scroll should include enough bottom padding so the
 * last content isn't hidden behind the tab bar.
 */
export const FLOATING_TAB_BAR_SCROLL_BOTTOM_PADDING = 140;

